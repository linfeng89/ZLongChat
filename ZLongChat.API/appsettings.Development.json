{
  // STUN / TURN 已统一到 appsettings.json 的 ZLongChat:Client 节，
  // 本文件只保留开发环境特有的日志级别覆盖。
  // 如需开发环境专用的 ICE 服务器，在这里加一段 "ZLongChat:Client:IceServers" 覆盖即可。
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore.SignalR": "Debug"
    }
  }
}
