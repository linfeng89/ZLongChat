# 即时情绪接住网络 · AI 原生版完整产品文档

> 版本：v2.1（已按评审意见修订）
> 形态：AI 原生 / MCP 协议 / 个人情绪代理 / 生成式轻界面
> 定位：一个可被任何智能体调用的情绪接住协议
> **主次：人类是最终归属，AI 只是让用户「有个缓冲、被接纳」的那一层**

> **本次修订要点**（相对 v2.0）：
> 1. **AI 定位更正**：从「AI 陪伴者」改为「**AI 缓冲接住**」—— 是过渡层，不是陪伴终点（第三节、第七节）
> 2. **隐私与危机冲突的解决**：常规即焚；**危机 / 举报两个例外，必须当场告知用户**（13.3、13.5、13.6、4.6）
> 3. **技术选型分两层**：MVP 层 / 愿景层分开，不再把 3–5 年后的技术当 MVP 选型（十四）
> 4. **服务端沿用现有 .NET 工程**，并给出与 `ZLongChat` 项目的逐项对应（14.1、14.3）
> 5. **MVP 只用文字**：不做语音、不做 TTS、不做端侧变声；ASR / 对象存储随之移出 MVP（13.1）
> 6. **指标与定位对齐**：北极星改为「真人接住率」；删除「AI 占比 >60%」这种反向激励指标（十六）
> 7. **冷启动补排期**：MVP 到可上线约 8–11 周（十五）
> 8. 修正全文小节编号、重画整体架构图

---

## 目录

1. 产品概述
2. 核心概念
3. 整体架构
4. 个人情绪代理
5. 情绪接住协议（MCP Server）
6. 情绪信号总线
7. AI 缓冲接住
8. 人类倾听者网络
9. 生成式轻界面
10. 情境触发
11. 功能需求
12. 核心流程与状态机
13. 安全与危机干预
14. 技术选型
15. 冷启动路径
16. 指标体系
17. 风险与应对
18. 附录

---

## 一、产品概述

### 1.1 一句话定位

> **一个树洞。** 你可以只是说出来，也可以让人看见，也可以让人陪你一会儿。
> 可被任何智能体调用，聊完即散，无关系沉淀。

**三个状态，由用户自己选：**

| 状态 | 名称 | 说明 |
| :---: | --- | --- |
| **1** | **说出来就好** | **不做任何展示**，只记录在用户端侧的一条数据 —— **平台看不到** |
| **2** | **被听到** | 进入树洞池，可以被看到/听到，**但不能被接住** |
| **3** | **需要陪伴** | 进入树洞池，可以被看到/听到，**且有接住功能** |

**平台义务随用户选择分层**：状态 1 平台完全不知情；状态 2 平台只是"墙"；状态 3 才涉及连接。

### 1.1.1 明确不做承诺

**产品不承诺任何响应指标。** 不做下列宣传：

| ❌ 不宣传 | 理由 |
| --- | --- |
| "1 分钟内被接住" | 抬高合理期待 = 抬高自身义务 |
| "保证有人陪你" | 供给不可控，承诺必然落空 |
| "100% 识别危机" | 法律上无法保证 |
| "提供心理支持" | 触碰《精神卫生法》资质红线 |

**只描述事实，不描述保证：**"你可以发出来，别人可以看到，愿意的人可以接住。"

### 1.2 产品背景

- 很多人有烦恼，但不想让熟人知道，找不到人倾诉。
- 传统心理咨询门槛高、费用高、预约难。
- 匿名社交容易变味，变成骚扰、约炮、诈骗。
- 现有倾诉平台多为接单模式，有供给方和需求方，关系可沉淀。
- AI 时代，用户不再需要打开 App，而是希望在任何智能体中表达意图即可获得服务。
- 需要一个临时、双盲、有边界、可监管、可被智能体调用的情绪陪伴协议。

### 1.3 产品目标

1. **提供一个低门槛的表达出口** —— 用户可以只是说出来（状态 1），不被任何人看到。
2. **提供被看见的可能** —— 用户可以选择被听到（状态 2），获得"有人看到了"的感觉。
3. **提供被陪伴的可能** —— 用户可以选择需要陪伴（状态 3），由自愿的倾听者接住。
4. 让倾听者可以随时在场、自愿接住、随时离开。
5. 聊完即散，无好友、无私信、无主页、无历史。
6. **不承诺响应、不承诺识别、不替代专业帮助** —— 只提供场所与机制。
7. 反成瘾，追求「说出来一次，然后回到生活」。
8. 可被任何支持 MCP 的智能体调用，无需独立 App。

> **与上一版的关键差异**：删除了「1 分钟内被接住」「AI 秒级接住」等**承诺性目标**。
> 产品从"保证接住"退回到"提供可能" —— 这既更诚实，也显著缩小了自身义务边界。

### 1.4 非目标

- 不做匿名社交。
- 不做心理咨询平台。
- 不做接单/抢单/积分/排行榜。
- 不做实时语音房（MVP）。
- 不做好友、私信、主页、动态。
- 不做公开曝光、挂人、审判。
- 不做 AI 伴侣，不制造情感依赖。
- AI 不替代人类陪伴，只做第一层兜底。
- 不做独立 App 作为唯一入口。

### 1.5 形态对比

| 维度 | 传统形态（App/小程序） | AI 原生形态 |
|---|---|---|
| 入口 | 打开应用 | 任意智能体、任意设备 |
| 交互 | 点击按钮 | 表达意图 |
| UI | 固定页面 | 生成式轻界面 |
| 身份 | 手机号注册 | 手机号实名 + 匿名 ID 分离（DID 仅愿景） |
| 分发 | 应用商店 | 智能体市场 / MCP 注册 |
| 通知 | 推送 | 情境触发 |
| 隐私 | 后台实名 | 常规即焚 + 危机/举报两个透明例外 |
| 供给 | 用户主动打开 | 代理接收轻信号 |
| 成瘾 | 需额外设计 | 天然反成瘾 |
| 扩展 | 平台中心 | 协议中心 |

---

## 二、核心概念

### 2.1 三个状态（核心）

用户发布时自己选择，**平台的角色与义务随之变化**：

| 状态 | 名称 | 去向 | 他人可见 | 可接住 | 平台角色 |
| :---: | --- | --- | :---: | :---: | --- |
| **1** | **说出来就好** | **仅用户端侧** | ❌ **不上传** | ❌ | **无**（平台不知情） |
| **2** | **被听到** | 树洞池 | ✅ 可看/听 | ❌ | **匿名墙**（只读广播） |
| **3** | **需要陪伴** | 树洞池 | ✅ 可看/听 | ✅ | **匿名墙 + 连接机制** |

**三个状态的实质区别：**

| 状态 | 用户的意图 | 平台知道什么 | 承担什么 |
| --- | --- | --- | --- |
| 1 | 我只是想说 | **什么都不知道** | 无内容义务（内容不在平台） |
| 2 | 我想被看见 | 这是一条想被看到的表达 | **内容审核**（法定） |
| 3 | 我想有人陪 | 用户在**寻求连接** | 内容审核 + 连接相关的轻度注意 |

> **设计原则：不多做，也不少做。**
> 状态 1 平台**不做任何处理**（也不做危机识别——因为看不到）；
> 状态 2 只做内容审核，**不提供接住入口**；
> 状态 3 才引入连接。

**状态一旦选定，不可变更：**

- 状态 1 是**纯端侧**的一条记录，不产生任何服务端数据。
- 状态 2、3 发布后进入树洞池，**两个方向都不可切换**。
- 想改主意？**删除后重新发布** —— 得到一个全新的 signalId，与旧的无任何关联。
  这既满足"改主意"的需求，也保住了「**用户说只要被听到，就绝不可能被接住**」这个硬保证。

> **为什么不做就地切换**：
> ① 从 2 升到 3 会让一条"只要被听到"的内容突然变得可被搭话，**违背用户当初的选择**；
> ② 从 3 降到 2 会让正在等待的倾听者白等，且可能被用来"钓鱼"。
> **不可变是最简单、也最不容易出错的规则。**
>
> 三个状态都**不建立关系**，聊完即散。

### 2.2 六个核心概念

| 概念 | 定义 |
|---|---|
| 个人情绪代理 | 用户授权的本地智能体，替用户表达、保护、筛选、管理 |
| 情绪接住协议 | MCP Server，暴露倾诉、接住、陪伴等工具，供任何智能体调用 |
| 情绪信号总线 | 事件驱动的信号网络，不派单、不抢单、自愿响应 |
| AI 缓冲接住 | **情感缓冲层（非陪伴终点）**：秒级接住、身份透明、稳住情绪、**目标是尽快交给真人** |
| 人类倾听者网络 | 真人自愿接住，更高质量的深度陪伴 |
| 生成式轻界面 | 按需生成、用完即焚的 UI 组件，嵌入宿主智能体 |

### 2.3 一句话解释

> 用户只需要表达，个人代理负责协商，情绪接住协议负责连接，情绪信号总线负责广播，AI 缓冲接住秒级接住兜底，人类倾听者自愿深度连接，生成式界面负责交互，聊完即散。

### 2.4 体验对比

**状态 1 · 说出来就好**

```text
用户对设备说："今天真是糟透了。"
（只记录在端侧，不上传，没有任何人会看到）
用户可以自己回看，也可以删掉。
```

**状态 2 · 被听到**

```text
用户想让人看到 → 生成式 UI 弹出发布面板 → 选「被听到」→ 发布
→ 进入树洞池，别人可以看到
→ 收到非互动反馈："3 个人看到了你的话"
（没有接住入口 —— 不会有人来搭话）
```

**状态 3 · 需要陪伴**

```text
→ 选「需要陪伴」→ 发布
→ 进入树洞池，可以被看到，也可以被接住
→ 有倾听者愿意接住 → 临时陪伴 → 聊完即散
```

> **三个状态由用户自己选，平台不替用户决定。**
> 平台也不承诺任何状态下的响应速度或结果 —— 只提供场所与机制。

---

## 三、整体架构

### 3.1 分层

```text
┌──────────────────────────────────────────────────────────────┐
│ 用户侧（任意设备 / 任意智能体）                                 │
│   手表 · 耳机 · 音箱 · 车机 · 手机 · 电脑 · 智能体对话           │
└───────────────────────────┬──────────────────────────────────┘
                            │ 表达意图
┌───────────────────────────▼──────────────────────────────────┐
│ 个人情绪代理（理解 / 保护 / 代发 / 筛选）                       │
│   · 管理授权 · 代发信号 · 筛选倾听者                            │
│   · MVP 先作为服务端组件，验证后再下沉端侧                      │
└───────────────────────────┬──────────────────────────────────┘
                            │ MCP / A2A
┌───────────────────────────▼──────────────────────────────────┐
│ 情绪接住协议层（MCP Server）                                   │
│   publish_signal · get_active_signals · catch_signal         │
│   ai_catch_signal · send_message · request_human             │
│   request_extension · end_session · report                   │
│   crisis_intervention                                        │
└───────────────────────────┬──────────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────┐
│ 情绪信号总线                                                  │
│   发布 → 广播 → AI 秒接(缓冲) → 真人自愿响应 → 临时陪伴 → 消散   │
│   不派单 · 不抢单 · 无积分 · 无等级 · 无排行榜                  │
└───────────┬───────────────────────────────┬──────────────────┘
            │                               │
┌───────────▼──────────────┐   ┌────────────▼──────────────────┐
│ AI 缓冲接住（过渡层）      │   │ 人类倾听者网络（核心）         │
│  · 秒级接住，不让用户空等  │   │  · 自愿响应，深度陪伴         │
│  · 情绪缓冲 + 被接纳感     │   │  · 代理接收轻信号             │
│  · 身份透明               │   │  · 人类只做最终同意           │
│  · 目标是尽快交给真人      │   │  · 质量更高，是最终归属        │
│  · 限时，不制造依赖        │   │                              │
└───────────────────────────┘   └──────────────────────────────┘
```

### 3.2 核心原则：人类是最终归属，AI 只是可选的缓冲

**去掉承诺之后，AI 的角色也需要重新定位。**

AI 不再是为了兑现「秒级接住」的承诺，而只是**在状态 3 下减少空等**：

| 原来的定位 | 修正后 |
| --- | --- |
| 兑现「1 分钟内被接住」的承诺 | **不承诺**。只是"如果暂时没人接，AI 可以先陪着" |
| 秒级接住是硬指标 | 尽力而为，**没有 SLA** |
| AI 缓冲时长是成本指标 | 仍然希望越短越好，但不再是"承诺未兑现" |

**AI 的边界不变：** 身份透明、只共情不建议、不诊断、不挽留、不制造连续感。

**三个状态下的 AI：**

| 状态 | 是否有 AI |
| --- | --- |
| 1 · 说出来就好 | ❌ 完全端侧，**没有 AI 参与**（也不需要） |
| 2 · 被听到 | ❌ 用户只要被看到，**不需要回应** |
| 3 · 需要陪伴 | ✅ 可选：暂无真人接住时，AI 可以先陪着 |

> **AI 只在状态 3 出现**，而且是**退而求其次的选项**，不是产品卖点。
> 这样既避免了"AI 陪伴"的伦理问题，也避免了"承诺了 AI 就一定在"的义务。

### 3.3 三个「轻」

| 维度 | 传统形态 | 本方案 |
| --- | --- | --- |
| 应用轻 | 装 App、注册账号 | 无 App，意图即入口 |
| 界面轻 | 固定页面 | 按需生成、用完即焚 |
| 关系轻 | 加好友、留历史 | 聊完即散，无沉淀 |
| **承诺轻** | 承诺响应、承诺陪伴 | **只描述事实，不描述保证** |

## 四、个人情绪代理

### 4.1 定义

> 个人情绪代理，是你授权的一个本地智能体。它替你表达情绪、保护隐私、筛选匹配、管理边界。它不陪你聊天，不给你建议，不替你做决定。你说话，它办事；你同意，它执行。

### 4.2 它不是什么

- 不是聊天机器人：不陪你聊天，不给建议，不扮演朋友。
- 不是心理咨询师：不诊断、不治疗、不干预。
- 不是 App：没有图标，没有界面，不主动打开。
- 不是平台账号：不代表平台，不代表你，只服务你。
- 不是 AI 伴侣：不产生情感依赖，不制造陪伴幻觉。

### 4.3 它住在哪里

| 位置 | 说明 | 隐私等级 |
|---|---|---|
| 手机端侧 | 最常见，手机本地运行 | 高 |
| 手表/耳机端侧 | 可穿戴设备本地 | 高 |
| 个人电脑端侧 | PC 本地运行 | 高 |
| 可信执行环境 | TEE / 安全芯片 | 最高 |
| 个人云 | 用户自己的云服务器 | 中高 |
| 平台托管 | 平台代管，用户授权 | 中 |

推荐顺序：端侧 > 个人云 > 平台托管。
MVP 阶段可以先用平台托管，但架构上必须为端侧预留。

### 4.4 倾诉者侧职责

| 职责 | 具体动作 |
|---|---|
| 保护隐私 | 匿名 ID、数据最小化、常规对话即焚 |
| 代发信号 | 把用户的选择转化为标准化信号，发布到总线 |
| 筛选倾听者 | 根据标签、信誉、历史筛选响应 |
| 管理会话 | 倒计时、延时请求、结束缓冲 |
| 安全兜底 | 规则匹配明显信号，展示热线卡片（**不宣称识别能力**） |
| 边界守护 | 不泄露身份、不交换联系方式、不留下历史 |

#### ⚠️ 明确不做：情绪理解

**原稿有「理解情绪 / 情绪识别 / 情绪摘要」等能力，已全部移除。** 理由如下。

**逐项追问"它到底干吗用"，四个候选用途都不成立：**

| 原本设想的用途 | 为什么不需要 |
| --- | --- |
| **自动打标签** | 用户自己点 2-3 个标签成本极低，而且**用户最清楚自己的状态**。AI 猜错了反而误导匹配 |
| **给倾听者的情绪摘要** | 有隐私代价（摘要经过 LLM、还要给陌生人看），而且 AI 缓冲时长本应很短，使用频率低 |
| **按情绪匹配** | 产品是「不派单、自愿响应」，**没有主动匹配**；标签匹配已经够用 |
| **情绪改善度量** | **直接问用户**（"现在感觉怎么样？"）比 AI 猜更准，也不涉及分析用户 |

**移除还带来三个额外好处：**

| 好处 | 说明 |
| --- | --- |
| 少一次 LLM 调用 | 降低延迟与成本 |
| 少一个数据字段 | 减小隐私面（少存一样东西） |
| **少一个暗示** | 不暗示"我们在分析你的情绪" —— 与"少承诺、少介入"的定位一致 |

> **为什么它会出现在文档里**：它是从原稿技术选型表里继承下来的
> （"情绪识别 \| 自研/第三方 \| 情绪分类，标签推荐"），
> 属于「**听起来 AI 产品应该有的能力**」，但从未对应到某个具体、不可替代的用途。
>
> **保留的例外**：AI 缓冲**内部**需要"共情"能力（听得懂情绪才能回应），
> 但那是**提示词层面的事**，不是一个独立的"情绪理解引擎"，也不产生额外数据。

### 4.5 倾听者侧职责

| 职责 | 具体动作 |
|---|---|
| 过滤骚扰 | 自动拦截高风险信号 |
| 匹配标签 | 根据偏好和精力推荐信号 |
| 轻提醒 | “有人需要听，你愿意接住吗？” |
| 防止耗竭 | 限制每日接住次数，提醒休息 |
| 管理会话 | 等待窗口、延时响应、结束 |
| 边界守护 | 不暴露身份、不被无限等待、不被道德绑架 |

### 4.6 授权模型

| 授权项 | 默认 | 用户可改 | 说明 |
|---|---|---|---|
| 读取情绪信号 | 关闭 | 可开 | |
| 读取生理信号 | 关闭 | 可开 | |
| 自动发布信号 | 关闭 | 可开 | |
| 自动筛选倾听者 | 开启 | 可关 | |
| 情境触发 | 关闭 | 可开 | |
| **安全兜底** | 开启 | 可关 | 展示热线卡片；**不留存、不告知、不宣称识别** |
| **举报留存** | 开启 | 不可关 | ⚠️ **唯一留存例外**：举报时保存该次对话，**并当场告知用户** |
| 数据留存（常规） | 关闭 | 不可开 | 常规对话仍聊完即焚 |

**原则**：默认最小授权，用户主动开启，随时可撤销。

> **两个不可关的例外，靠「透明告知」而非「静默执行」来取得正当性。**
> 用户无法关闭危机干预与举报留存（这是法律与安全底线），
> 但**有权在发生的那一刻知道**，并了解保存范围与期限。
> 详见 13.3 / 13.5 / 13.6。

### 4.7 技术形态

**MVP 阶段：**

| 组件 | 实现 |
|---|---|
| 运行环境 | 手机端侧（iOS/Android） |
| 语音 | ❌ MVP 不做（仅文字），v2 评估（见 13.1） |
| 授权管理 | 本地授权列表 |
| 通信 | MCP Client 调用 MCP Server |
| 存储 | 本地 SQLite，聊完即删 |
| UI | 无独立 UI，嵌入宿主智能体 |

**进阶阶段：**

| 组件 | 实现 |
|---|---|
| 运行环境 | TEE / 安全芯片 |
| 隐私 | 匿名 ID + 常规即焚（ZKP/DID 仅愿景，见 14.2） |
| 通信 | A2A + MCP |
| 存储 | 本地加密 + 可验证删除 |
| UI | 生成式轻界面 |

---

## 五、情绪接住协议（MCP Server）

### 5.1 定义

不做独立应用，做 MCP 服务器。任何支持 MCP 的智能体都能调用。

### 5.2 暴露的工具

| 工具 | 功能 | 调用方 |
|---|---|---|
| `publish_signal` | 发布倾诉信号 | 倾诉者代理 |
| `get_active_signals` | 获取当前信号流 | 倾听者代理 |
| `get_signal_detail` | 查看某条信号详情（**MVP 为文字**） | 倾听者代理 |
| `catch_signal` | 接住某人，进入临时陪伴 | 倾听者代理 |
| `ai_catch_signal` | AI 自动接住，进入 AI 缓冲 | 系统 / AI 缓冲模块 |
| `send_message` | 临时陪伴中发消息 | 双方代理 |
| `request_human` | AI 缓冲中呼叫真人切换 | 倾诉者代理 / AI 模块 |
| `switch_to_human` | 真人接入，切换陪伴主体 | 系统 |
| `request_extension` | 请求延时 15 分钟 | 倾诉者代理 |
| `respond_extension` | 同意/拒绝延时 | 倾听者代理 |
| `end_session` | 结束陪伴 | 双方代理 |
| `report` | 举报 | 双方代理 |
| `crisis_intervention` | 触发危机干预 | 系统自动 |

### 5.3 暴露的资源

| 资源 | 内容 |
|---|---|
| `signal://active` | 当前活跃信号列表 |
| `session://current` | 当前临时陪伴会话 |
| `ui://publish` | 倾诉发布 UI 组件 |
| `ui://signal-detail` | 信号详情卡组件（**MVP 为文字**） |
| `ui://chat` | 临时聊天 UI 组件（支持 AI/人类模式切换） |
| `ui://ai-badge` | AI 缓冲标识组件 |
| `ui://human-switch` | 真人切换确认组件 |
| `ui://crisis` | 危机干预 UI 组件 |

### 5.4 工具调用示例

**发布倾诉：**

```json
{
  "tool": "publish_signal",
  "params": {
    "type": "text",
    "content": "深夜了，还是睡不着……",
    "tags": ["孤独", "深夜"],
    "mode": "companion"
  }
}
```

> MVP 无 `audio` 字段。语音为 v2 预留（见 13.1）。

**接住信号：**
```json
{
  "tool": "catch_signal",
  "params": {
    "signal_id": "sig_abc123",
    "first_message": "你好，我在听。"
  }
}
```
---

## 六、情绪信号总线
### 6.1 定义
不建平台，建信号总线。倾诉者发出情绪事件，不同智能体订阅并响应。

### 6.2 信号结构

> **状态 1（说出来就好）不产生 Signal** —— 它只是用户端侧的一条本地记录，不上传。

```ts
Signal {
  id: string
  type: "text"                      // MVP 仅文字；"voice" 为 v2 预留
  content: string
  tags: string[]

  // 对应三个状态中的后两个
  //   "broadcast" = 状态 2 被听到   —— 进池，可看/听，**不可接住**
  //   "companion" = 状态 3 需要陪伴 —— 进池，可看/听，**可接住**
  mode: "broadcast" | "companion"

  created_at: timestamp
  expires_at: timestamp
  status: "active" | "seen" | "catching" | "caught" | "in_session" | "expired"
  retention: "ephemeral" | "report_saved"   // 留存状态，见 13.3
}
```

**两种 mode 的关键差异：**

| | `broadcast`（状态 2） | `companion`（状态 3） |
| --- | --- | --- |
| 进入树洞池 | ✅ | ✅ |
| 可被查看/收听 | ✅ | ✅ |
| **可被接住** | ❌ **不提供接住入口** | ✅ |
| 平台角色 | 匿名墙 | 匿名墙 + 连接机制 |
| 平台义务 | 内容审核 | 内容审核 + 连接注意 |

> **`broadcast` 不提供接住入口是硬规则**，不是 UI 层面的隐藏 ——
> 服务端在 `catch_signal` 时校验 mode，对 `broadcast` 直接拒绝（`MODE_NOT_CATCHABLE`）。
> 这保证了"用户说只要被听到，就不会被打扰"是**机制保证**而非**界面约定**。
### 6.3 总线规则
• 不派单：不指定谁接。

• 不抢单：不设先到先得。

• 无积分：不奖励接住。

• 无等级：不排名。

• 自愿响应：谁愿意，谁响应。

• 先到先得：多人响应时，第一个发送第一句话的人获胜。

### 6.4 信号生命周期

发布 → 广播 → 被看见 → 被收听 → AI 秒接
                          │         │
                          │         ├─ AI 缓冲中 → 呼叫真人 → 真人接入 → 人类陪伴
                          │         └─ AI 缓冲结束 → 消散
                          ↓
                    人类正在接 → 已接住
                          │
                          ▼
                    临时陪伴（人类）
                          │
                          ▼
                    结束缓冲
                          │
                          ▼
                    消散（聊完即焚）

---

## 七、AI 缓冲接住（仅状态 3，可选的安慰剂层）

### 7.1 定位

> **AI 只在状态 3「需要陪伴」下出现，且只是"暂无真人时的过渡"，不是产品卖点。**

| 项 | 说明 |
| --- | --- |
| 何时出现 | **仅状态 3**，且暂时没有倾听者愿意接住时 |
| 是否承诺 | ❌ **不承诺**。AI 是"尽力而为"的兜底，不是 SLA |
| 何时退出 | 真人接住 → AI 立即退出 |
| 状态 1 / 2 | **完全不出现在这两个状态下**（状态 1 在端侧，状态 2 用户只要被看到） |

核心原则：**身份透明、用户可选择、尽快交给真人、指标越低越好。**

- 明确告知用户「这是 AI」，绝不冒充人类。
- 用户可随时拒绝 AI、呼叫真人、或直接结束。
- **AI 的目标是让自己变得多余** —— 真人一到就交接退出。

> **为什么不把它当卖点**：一旦宣传"AI 随时陪你"，就形成了持续在线的承诺与义务。
> 现在的定位是"如果暂时没人，可以先有个回应"—— 这是描述事实，不是保证。

### 7.2 AI 缓冲的边界

✅ **应该做的：**
- 共情式倾听：「听起来你真的很难受。」
- 情绪确认：「这件事让你觉得很委屈对吗？」
- 引导表达：「愿意多说说吗？」
- 正常化：「遇到这种事，谁都会不好受的。」
- 危机识别与转介：识别高危信号，立即介入。
- **主动推动转真人**：「要不要试试和一位真人聊聊？」

❌ **绝对不能做的：**
- 给出具体建议：「你应该分手/辞职/……」
- 心理诊断：「你可能有抑郁症。」
- 价值评判：「你这样不对。」
- 假装人类：用「我也经历过」之类的虚假个人经历。
- **挽留用户**：不主动延长对话、不制造「再聊一会儿」。
- **制造连续感**：不主动记忆、不主动问候、不说「我一直都在」。
- 长时间深度陪伴：单次 AI 缓冲**建议 10–15 分钟**，到时主动引导转真人。

> **与「人类为主」的一致性**：AI 越成功，用户越快被交给真人。
> 因此「AI 缓冲时长」是**成本指标**，不是成绩指标（见 16.2）。

### 7.3 交互模式

#### 模式一：AI 先接住 + 后台找真人（默认）

用户发布信号后，AI 立即接住，同时后台继续广播信号给真人倾听者。

```
用户发布信号
    ↓
AI 秒级接住（明确告知身份）
    ↓
后台同时广播给真人倾听者
    ↓
用户可选：
  ├─ 等真人（默认） → AI 缓冲陪伴 → 真人接入后无缝切换
  ├─ 主动呼叫真人 → 立即加入真人队列，AI 继续稳住情绪
  └─ 拒绝 AI → 仅真人模式排队，无 AI 对话
```

#### 模式二：纯 AI 缓冲（例外路径）

用户明确选择「只想和 AI 说」，不进入真人广播队列。
适用于：只想快速释放情绪、不信任陌生人、深夜无人在线等场景。

> 这是**例外**，不是默认。产品仍以「交给真人」为目标，但尊重用户不愿与真人连接的意愿。

#### 模式三：AI 缓冲 + 真人接力

AI 先稳住情绪，同时后台留意愿意接住的人。
真人接入时可快速理解上下文，避免用户重复讲述。

### 7.4 真人切换机制

| 触发点 | 说明 | 优先级 |
| --- | --- | --- |
| **真人已就位** | 匹配到倾听者，立即询问是否切换 | **最高** |
| 用户主动呼叫 | AI 缓冲中，用户说「我想和真人聊」 | 高 |
| 情绪加重 | AI 检测到情绪持续恶化，主动建议真人接入 | 高 |
| 话题越界 | 涉及创伤、自伤等，立即转人工 | 高 |
| 时长到期 | AI 缓冲满 10–15 分钟，主动引导转真人 | 中 |

切换时：
- 询问用户是否同意将摘要同步给真人。
- 真人接入后，**AI 立即退出**，标识切换为「倾听者」。

### 7.5 身份标识设计

- **AI 缓冲时**：头像旁持续显示「AI」标签，消息气泡颜色略有区分。
- **切换真人后**：标签变为「倾听者」，气泡颜色恢复正常。
- **全程透明**：绝不混淆身份，绝不以 AI 冒充人类。

### 7.6 危机干预升级

AI 识别到危机信号时，比真人模式更严格地执行转介：

- 立即触发 12356 热线转介弹窗。
- 同时保留对话，持续安抚用户。
- 通知人工审核团队介入。
- 不尝试自行处理危机，只做「稳住 + 转介」。
- **明确告知用户：本次对话将保存至服务端**（见 13.5）。
## 八、人类倾听者网络
### 8.1 定位
人类倾听者网络是核心，AI 只做辅助。不替代人类陪伴，不制造情感依赖。

### 8.2 倾听者体验

倾听者代理：“有人需要听，标签是孤独，你愿意接住吗？”
倾听者：“愿意。”
代理自动进入等待窗口，倒计时 3 分钟。
倾诉者进入 → 临时陪伴开始。

### 8.3 倾听者保护
• 每日最多接住 5 次。

• 等待窗口 3 分钟，超时自动释放。

• 可随时结束陪伴。

• 可拒绝延时，不惩罚。

• 可屏蔽标签。

• 可开启勿扰模式。

• 代理自动过滤骚扰信号。

### 8.4 角色动态切换
• 每个人既可以是倾诉者，也可以是倾听者。

• 一个用户最多同时有一个倾听会话和一个倾诉会话。

• 正在倾听 A，又被 B 接住，B 作为倾诉自我进入消息列表，不强制切换。

---

## 九、生成式轻界面

### 9.1 定义

不是没有界面，而是界面按需生成、用完即焚。

### 9.2 生成方式

MCP Server 暴露 UI 资源（HTML/CSS/JS 组件）。

宿主智能体在对话中渲染为沙箱 iframe。

任务结束，组件销毁，不留下任何痕迹。

### 9.3 核心 UI 组件

| 组件 | 触发 | 内容 |
|---|---|---|
| 倾诉发布面板 | 用户表达倾诉意图 | **文字**、标签、模式 |
| 信号详情卡 | 点击信号 | 全文、接住按钮、关闭 |
| 接住输入面板 | 点击接住 | 输入框、录音、发送 |
| 等待窗口 | 接发送第一句话 | 倒计时、结束等待 |
| 临时聊天 | 双方进入 | 消息流、倒计时、延时、结束 |
| AI 缓冲标识 | AI 缓冲模式 | 头像旁"AI 缓冲"标签、气泡颜色区分 |
| 真人切换按钮 | AI 缓冲中常驻 | "呼叫真人"按钮、切换状态提示 |
| 切换确认弹窗 | 真人接入时 | 是否切换确认 |
| 结束缓冲 | 会话结束 | 10 秒倒计时、举报 |
| 危机干预 | 触发危机词 | 热线、呼吸引导、安全计划 |

### 9.4 示例：倾诉发布面板

```html
<div class="treehole-publish">
  <h3>倾诉</h3>
  <div class="tags">
    <span>学业</span>
    <span>关系</span>
    <span>孤独</span>
    <span>焦虑</span>
    <span>家庭</span>
    <span>其他</span>
  </div>
  <div class="mode">
    <label><input type="radio" name="mode"> 只想被听到</label>
    <label><input type="radio" name="mode"> 希望有人接住</label>
  </div>
  <textarea placeholder="想说什么都可以…"></textarea>
  <p class="notice">发布后 1 分钟内一定有回应。陪伴你的可能是 AI，也可能是真人志愿者。</p>
  <button class="publish">发布</button>
</div>
```

### 9.5 安全
• 沙箱 iframe 运行，与宿主隔离。

• 严格内容安全策略（CSP）。

• 双向通信需用户授权。

• 数据最小化，聊完即焚。

---

## 十、情境触发

### 10.1 定义

不再依赖用户主动打开应用，而是环境感知、无感触发。

### 10.2 触发源

| 设备 | 信号 | 触发 |
| --- | --- | --- |
| 手表 | 心率异常 + 深夜 | "要不要找个树洞？" |
| 耳机 | 语音颤抖 | "我可以帮你接住。" |
| 音箱 | 长时间沉默 | "需要聊聊吗？" |
| 车机 | 长时间沉默 | 播放呼吸引导 |
| 手机 | 打字节奏异常 | 轻提醒 |
| 智能体 | 对话情绪低落 | 建议接入 |

### 10.3 触发原则

• 不打扰：轻提醒，不强制。

• 可忽略：用户不回应，不重复。

• 可关闭：设置里可关闭情境触发。

• 隐私优先：生理信号端侧处理，不上传。

---

## 十一、功能需求

### 11.1 功能清单

| 编号 | 模块 | 功能 | MVP | 优先级 |
| --- | --- | --- | --- | --- |
| F01 | 身份 | 手机号实名 + 匿名 ID 分离 | ✅ | P0 |
| F02 | 个人代理 | 隐私保护、代发信号、管理授权 | ✅ 服务端组件 | P0 |
| F03 | MCP Server | 暴露工具与资源 | ✅ | P0 |
| F04 | 信号总线 | 发布、广播、响应 | ✅ | P0 |
| F05 | 生成式 UI | 倾诉发布面板（**文字**） | ✅ | P0 |
| F06 | 生成式 UI | 信号详情卡（**替代原播放浮窗**） | ✅ | P0 |
| F07 | 接住 | 正在接、已接住两个状态 | ✅ | P0 |
| F08 | 等待窗口 | 3 分钟等待倾诉者进入 | ✅ | P0 |
| F09 | 临时陪伴 | 消息列表、聊天、倒计时 | ✅ | P0 |
| F10 | 延时 | 倾诉者发起、倾听者同意、3次×15分钟 | ✅ | P0 |
| F11 | 结束缓冲 | 10 秒缓冲期、举报保护 | ✅ | P0 |
| F12 | 举报 | 详情卡、聊天页、缓冲期举报 | ✅ | P0 |
| F13 | 安全兜底 | 规则匹配、展示热线卡片（**不宣称、不留存**） | ✅ | P0 |
| F14 | **语音方案** | **MVP 不做**，v2 评估（见 13.1） | ❌ | **P2** |
| F15 | 限次 | 倾诉 3 次/日、倾听 5 次/日 | ✅ | P0 |
| F16 | 情境触发 | 设备信号触发 | ❌ | P2 |
| F17 | 授权管理 | 用户可查看、修改、撤销授权 | ✅ | P0 |
| F18 | 倾听者保护 | 过滤、限次、勿扰 | ✅ | P0 |
| F19 | 设置 | 隐身、勿扰、屏蔽、隐私、黑名单 | ✅ | P1 |
| F20 | 感受反馈 | 结束后**直接问用户**感受与安全感（不靠 AI 猜） | ✅ | P1 |
| F21 | **AI 缓冲接住** | 共情回应、不做建议、不诊断、**不挽留**（**仅状态 3**） | ✅ | P0 |
| F22 | 身份透明化 | 开场明确告知 AI 身份，对话中持续标识 | ✅ | P0 |
| F23 | 真人切换 | 真人就位即询问是否切换 | ✅ | **P0** |

> **MVP 列的意义**：明确哪些不进 MVP 排期。原稿把语音（F14）列为 P0，
> 会把 MVP 拖入端侧 SDK / 音频审核 / 对象存储的复杂地带，而它并不验证核心假设。
>
> **F23 从 P1 提到 P0**：真人切换是「人类为主」定位的关键路径，不是可选优化。

### 11.2 核心流程

倾诉者流程：
```text
情境触发 / 主动表达
→ 代理询问：想被听到 / 希望有人接住
→ 代理发布信号到总线
→ 信号广播给倾听者代理
→ AI 秒级接住（明确告知身份）
    ├─ 用户接受 AI 缓冲 → 持续对话 → 可随时呼叫真人 → 真人接入后切换
    ├─ 用户等待真人 → AI 陪聊同时后台排队 → 真人接入后无缝切换
    └─ 用户拒绝 AI → 仅真人模式排队
→ 生成式 UI 弹出接住提醒
→ 倾诉者进入临时陪伴
→ 聊天 / 延时 / 结束
→ 聊完即散，界面消失
```

倾听者流程:
```text
倾听者代理接收轻信号
→ “有人需要听，你愿意接住吗？”
→ 倾听者同意
→ 生成式 UI 弹出接住输入面板
→ 输入第一句话，发送
→ 等待倾诉者进入（3 分钟）
→ 临时陪伴开始
→ 聊天 / 延时请求 / 结束
→ 聊完即散，界面消失
```

---

## 十二、核心流程与状态机

### 12.1 泡泡状态机

```stateDiagram-v2
    [*] --> 已发布: 发布成功
    已发布 --> 正在被看见: 进入他人主视线
    正在被看见 --> 有人正在听: 有人点击播放
    有人正在听 --> 有人听完了: 播放完成≥95%
    有人听完了 --> 正在接: 倾听者点接住
    正在接 --> 已接住: 倾听者发送第一句话
    正在接 --> 恢复池中: 60秒未发送/取消
    恢复池中 --> 正在被看见: 继续上浮
    已接住 --> 等待进入: 会话创建
    等待进入 --> 陪伴中: 倾诉者3分钟内进入
    等待进入 --> 已释放: 3分钟超时
    陪伴中 --> 已结束: 倒计时归零/一方结束
    陪伴中 --> 已延时: 倾听者同意延时
    已延时 --> 陪伴中: 继续聊天
    已结束 --> 已销毁: 10秒缓冲无举报
    已结束 --> 冻结审核: 举报
    已释放 --> [*]
    已销毁 --> [*]
    冻结审核 --> [*]
    无人接住 --> 转树洞: 30-60分钟无接住
    转树洞 --> [*]

```

### 12.2 临时房状态机

```stateDiagram-v2
    [*] --> 等待进入: 倾听者发送第一句话
    等待进入 --> 陪伴中: 倾诉者3分钟内进入
    等待进入 --> 已释放: 3分钟超时
    等待进入 --> 已释放: 倾听者结束等待
    等待进入 --> 已释放: 倾诉者选稍后/不用了
    陪伴中 --> 延时请求中: 倾诉者发起延时
    延时请求中 --> 陪伴中: 倾听者同意(+15min)
    延时请求中 --> 陪伴中: 倾听者拒绝(继续原倒计时)
    延时请求中 --> 陪伴中: 30秒未响应(视为拒绝)
    陪伴中 --> 结束缓冲: 倒计时归零/一方结束
    结束缓冲 --> 已销毁: 10秒无举报
    结束缓冲 --> 冻结审核: 举报
    陪伴中 --> 冻结审核: 直接举报
    陪伴中 --> 危机干预: 触发危机词
    危机干预 --> 冻结审核: 人工处理
    已销毁 --> [*]
    已释放 --> [*]
    冻结审核 --> [*]
```

### 12.3 用户角色状态机

```stateDiagram-v2
    [*] --> 空闲
    空闲 --> 倾听中: 接住他人
    空闲 --> 倾诉中: 被他人接住
    倾听中 --> 空闲: 结束陪伴
    倾诉中 --> 空闲: 结束陪伴
    倾听中 --> 倾听中: 不能同时接住2人
    倾诉中 --> 倾诉中: 不能同时向2人倾诉
    倾听中 --> 倾听中_倾诉中: 被他人接住
    倾诉中 --> 倾听中_倾诉中: 接住他人
    倾听中_倾诉中 --> 倾听中: 结束倾诉会话
    倾听中_倾诉中 --> 倾诉中: 结束倾听会话
    倾听中_倾诉中 --> 空闲: 两个会话都结束
```
    
### 12.4 延时请求状态机
```stateDiagram-v2
    [*] --> 未发起
    未发起 --> 请求中: 倾诉者点需要
    请求中 --> 已同意: 倾听者同意
    请求中 --> 已拒绝: 倾听者点暂不延长
    请求中 --> 已拒绝: 30秒未响应
    已同意 --> 陪伴中: 倒计时加15分钟
    已拒绝 --> 陪伴中: 继续原倒计时
    已同意 --> 成功延时: 扣减一次
    已拒绝 --> 次数不变: 不扣次数
    成功延时 --> 未发起: 剩余次数大于0
    成功延时 --> 已用完: 剩余次数等于0
    次数不变 --> 未发起
    已用完 --> [*]
```
    
### 12.5 举报状态机
```stateDiagram-v2
    [*] --> 无举报
    无举报 --> 结束缓冲: 一方结束
    结束缓冲 --> 无举报: 10秒无举报
    结束缓冲 --> 举报中: 点击举报
    无举报 --> 举报中: 直接举报
    举报中 --> 冻结审核: 提交举报
    冻结审核 --> 已处理: 人工审核
    已处理 --> [*]
    无举报 --> 已销毁: 正常结束
    已销毁 --> [*]
```
---
    
## 十三、安全与危机干预

### 13.1 语音方案（MVP 决策：只用文字）

> **MVP 决策：只做文字倾听，不做语音、不做 TTS、不做端侧变声。**
> 语音相关能力（含变声）整体移到 v2 评估。

**MVP（只用文字）：**

• 倾诉与陪伴全程文字。

• **没有原声** —— 用户不录音，从根上消除声纹与音频留存风险。

• **文本天然可审** —— 审核与危机识别直接作用于文本，链路最短、精度最高。

• 服务端**零音频存储** —— 对象存储、ASR、TTS 在 MVP 全部不需要。

**v2 再评估的语音方案（三选一）：**

| 方案 | 隐私 | 审核 | 成本 | 说明 |
| --- | --- | --- | --- | --- |
| 端侧变声 + 上传变声音频 | 中（无原声，但韵律在） | **差**（ASR 作用于变声音频） | 高（端侧 SDK、功耗） | 原稿方案 |
| 上传原声 + 服务端 ASR | 差（原声出设备） | 好 | 中 | 与「原声不出设备」冲突 |
| **文字 + 服务端 TTS 合成语音** | **高（根本没有原声）** | **好（审文本）** | 低 | **推荐** |

> 端侧变声的三个问题：① 移动端/可穿戴端成本高、功耗大；
> ② **变声后音频仍带情绪韵律**，声纹匿名而情绪暴露；
> ③ 审核更差。
>
> 代价：丢失真人语音的情绪承载。这是真实损失，但 MVP 阶段文字足够验证核心假设
> （**有没有人愿意被接住 / 愿意接住别人**），语音放 v2。

### 13.2 匿名凭证

**MVP 决策：手机号实名 + 匿名 ID 分离。DID / VC / ZKP 移到后期（见 14.2）。**

**MVP 做法：**

• 前台匿名：匿名 ID 与实名 ID 分离，服务端仅存映射。

• **匿名 ID 定期轮换** —— 建议**每次会话结束轮换一次**，且最长 7 天强制轮换。
  比单纯「定期」更强：即使两次会话内容都泄露，也无法证明出自同一人。

• 后台可追溯：监管或司法需要时，可由服务端从映射还原真实身份。

• 不泄露真实身份：界面、日志、对端任何位置都不出现手机号。

**为什么 MVP 不用 DID / ZKP：**

> ⚠️ DID 的设计目标是**去中心化、无可信第三方**，而国内监管要求平台**可追溯到真实身份** ——
> 二者方向相反。共存需要一层「DID ↔ 实名」的受控映射，工程量与合规论证都不小。
>
> ZKP 解决的是「证明而不泄露」，但这里的核心诉求恰恰是「监管可追溯」—— 方向同样相反。
>
> **结论**：MVP 用「手机号实名 + 匿名 ID 分离」即可满足「前台匿名 / 后台可追溯」，
> 不引入额外工程与合规风险。DID / ZKP 列入愿景层，待生态阶段再评估。

### 13.3 聊完即焚（只有一个例外）

**默认行为：**

• 状态 1 的内容**从来不上传**，只存在用户端侧。

• 房间结束即删。

• 消息记录不存。

• 无好友、无私信、无历史。

**唯一例外（且必须当场告知用户）：**

| 例外 | 触发 | 保存什么 | **是否告知用户** |
| --- | --- | --- | --- |
| **举报** | **任一方主动提交举报** | 该次会话最后 20 条消息 | ✅ **立即告知** |

> **为什么只剩一个例外**：安全兜底（13.5）不额外留存内容。
> 留存只由**用户主动行为**（举报）触发 —— 这样例外具有最明确的正当性，
> 也不需要向用户解释"我们为什么在监控你"。

> **设计原则：例外可以存在，但绝不静默。**
> 用户有权在内容被保存的那一刻就知道。
>
> **告知文案**（见 18.1）：
> 「举报已提交。本次对话将保存至服务端，用于安全审核。」

### 13.4 内容审核

**审核范围随三个状态变化 —— 状态 1 平台看不到内容，因此不审核。**

| 状态 | 平台是否看到内容 | 审核 |
| :---: | --- | --- |
| **1 · 说出来就好** | ❌ **看不到**（纯端侧） | **无**（没有可审的对象） |
| **2 · 被听到** | ✅ 内容进池 | ✅ 内容审核（**法定**） |
| **3 · 需要陪伴** | ✅ 内容进池 + 会话消息 | ✅ 内容审核（**法定**） |

**审核分层（仅状态 2 / 3）：**

| 层 | 手段 | 覆盖 | 延迟 | 模式 |
| --- | --- | --- | --- | --- |
| **L0 规则** | 手机号 / 微信号 / QQ / URL / 违禁词 | **100% 消息** | < 1ms | **同步** |
| **L1 安全兜底** | 见 13.5（**独立通路**） | **100% 消息** | 规则同步 | 独立 |
| **L2 云端审核** | 文本内容安全 API | **100% 消息** | 200–500ms | **异步** |
| **L3 人工复审** | 人 | 仅命中项 + 举报 | — | — |

**为什么 L2 异步**：会话是双盲私密的（危害面小），异步换来**零延迟**与
**「审核 API 故障不会让全站聊不了天」**。

**为什么「发布」同步**：内容进池、可能被多人看到、频率低，一次审核收益最高。

**「AI 还是人工」不是二选一，是 AI 筛 + 人工判：**

| 角色 | 覆盖 | 优势 | 劣势 |
| --- | --- | --- | --- |
| AI（L0 + L2） | 100% | 零延迟、成本低、不漏扫 | 误判率高、不懂语境 |
| 人工（L3） | 仅命中项（< 1%） | 准确、能判语境 | **无法覆盖全量** |

**成本论据**：日活 1000、人均 30 条 = 3 万条/天。
全人工需 **10–15 名审核员**（不可持续）；AI 全量 + 人工复审命中项 ≈ **300 条/天，1 人可覆盖**。

**产品硬规则**：联系方式交换（手机号 / 微信号 / QQ / URL）**硬拦截并提示** ——
这是「聊完即散、不沉淀关系」的技术保障。

> 完整设计（双通路时序、故障降级、成本模型）见 `docs/审核与危机识别设计.md`。

### 13.5 安全兜底（原「危机干预」）

> **改名与降级**：从「危机干预系统」降为「**安全兜底**」。
> 平台**不宣称**具备危机识别能力，**不承诺**识别率 —— 只做"看到了就递一张纸条"。

**做法（很轻）：**

• **只看得到的内容**：状态 1 在端侧，平台看不到，因此**不做任何处理**。

• A1 规则匹配（同步，<1ms）→ 命中即展示热线资源。

• 展示内容：12356 心理援助热线 / 110 / 120 + 呼吸引导。

• 不诊断、不治疗、不替代专业机构。

• **不额外留存**：安全兜底不触发内容落盘。留存只有一个例外 —— **举报**（见 13.6）。

**明确不做的：**

| ❌ 不做 | 理由 |
| --- | --- |
| 不宣称"危机识别" | 一旦宣称就有持续履行的义务 |
| 不承诺识别率（含 100%） | 法律上无法保证 |
| 不做语义模型全量识别（MVP） | 成本高，且会强化"我们在监控你"的印象 |
| 不自动留存内容 | 留存会引入隐私与合规负担，且无用户主动授权 |
| 不承诺人工介入 | MVP 无 7×24 值守能力 |

**端侧可选的善意（不上传、不留存）：**

状态 1 的内容虽然平台看不到，但**端侧可以内置静态热线资源**（12356 / 110 / 120），
在任何界面都能一键取到。这是纯本地资源，**不涉及任何上传或识别**。

> **这个定位变化的意义**：
> 平台从"我负责识别你的危机"退回到"**我这里有一张写着热线的纸条，你需要就拿走**"。
> 前者是承诺，后者是善意 —— 前者会产生义务，后者不会。
>
> 但这**不意味着不该做**：成本极低（一张静态卡片），而一旦真的帮到一个人，价值是无限的。

### 13.6 举报保护

• 结束前 10 秒缓冲期。

• 举报后倒计时停止。

• 保留最后 20 条消息供审核（**MVP 无音频**）。

• 被举报方不提示举报人身份，避免报复。

• **举报方提交时明确告知：本次对话将保存至服务端用于审核。**

> 注意区分两种「不提示」：
> **不提示被举报方「谁举报的」**（防报复）≠ **不提示举报方「内容会被保存」**（必须提示）。

### 13.7 未成年人保护
• 通用版默认 18+。

• 校园版单独做，需学校主导、家长知情、严格分级。

• 中学版禁用实时语音与匿名池；大学版可先做文字匿名倾诉，需学校主导、家长知情。

### 13.8 防沉迷
• 每日倾诉 3 次，倾听 5 次。

• 无信息流，无限滚动。

• 不追求停留时长。

• 到时间提示休息。
---

## 十四、技术选型

> **分两层看**：MVP 层是「现在就要动手的」，愿景层是「方向对但今天不做」的。
> 原稿把两层混在一起，导致选型看起来像 3–5 年后的技术清单，无法据此排期。

### 14.1 MVP 层（现在做）

#### 协议层

| 类别 | 选型 | 理由 |
| --- | --- | --- |
| 智能体协议 | **MCP（官方 C# SDK）** | 标准化工具调用；与现有 .NET 服务同进程 |
| UI 协议 | **MCP Apps（`ui://` 资源）** | 生成式轻界面，宿主渲染为沙箱 iframe |
| 代理间协议 | ❌ **MVP 不做** | A2A 极早期；单代理直连 MCP Server 即可 |

#### 个人代理

| 组件 | 实现 | 说明 |
| --- | --- | --- |
| 运行环境 | **服务端组件**（`ZLongChat.Proxy`） | ⚠️ **MVP 不下沉端侧** —— 端侧模型 + TEE + 设备适配会拖死 MVP |
| 授权管理 | 服务端授权列表 | 接口设计为可迁移到端侧 |
| 通信 | MCP Client → MCP Server | 标准路径 |
| 存储 | PostgreSQL（授权）/ Redis（会话态） | 常规对话不落盘 |
| UI | 无独立 UI | 嵌入宿主智能体 |

> **端侧下沉的时机**：等协议与供给两侧验证通过、且端侧小模型能力达标后再做。
> 授权模型（4.6）与代理接口从第一天就按「将来可迁移到端侧」设计。

#### 服务端

**结论：沿用现有 .NET 工程，不另起技术栈。**

| 类别 | 原稿选型 | **MVP 选型** | 理由 |
| --- | --- | --- | --- |
| 语言 | Go / Node.js | **.NET 10（C#）** | 现有 6 个项目已实现实时通信 / 存储抽象 / 配置下发 / AI 抽象 |
| 框架 | Gin / NestJS | **ASP.NET Core Minimal API（已有）** | 现有 `ZLongChat.API` 直接扩展 |
| MCP Server | Spring AI / MCP SDK | **`ModelContextProtocol.AspNetCore`**（支持 net8.0 / net9.0 / net10.0） | 与现有 API 同进程，无需跨语言桥接 |
| 实时通信 | WebSocket / MQTT | **SignalR（已有 `ChatHub`）** | 已实现转发 / 离线队列 / 强类型客户端；widget 走 `tools/call`，**不需要 MQTT** |
| 信号总线 | Kafka / NATS | **Redis Streams**（起步进程内 Channel） | 试点规模用不上 Kafka |
| 缓存 | Redis | **Redis** | 在线池、房间倒计时、限次 —— 唯一真正的热点 |
| 数据库 | PostgreSQL | **PostgreSQL + EF Core** | 用户、房间、举报、延时 |
| 对象存储 | COS / OSS | ❌ **MVP 不需要** | 语音整体移出 MVP（见 13.1），**没有音频要存** |
| 身份 | DID + VC | **手机号实名 + 匿名 ID 分离** | DID 与「后台实名可追溯」方向相反（见 13.2） |

#### AI 与音频（MVP）

| 类别 | 选型 | 说明 |
| --- | --- | --- |
| AI 缓冲对话 | 大模型 + 共情提示词 | **必须带身份标识**；只共情不建议、不诊断 |
| 危机识别 | **规则（同步）+ 语义（准同步）双通路** | 是法律责任，纯关键词必然漏判；**放服务端**，见 13.5 |
| 内容审核 | **L0 规则（同步）+ L2 云内容安全（异步）** | 分层设计，见 13.4 与 `docs/审核与危机识别设计.md` |
| ASR | ❌ **MVP 不需要** | 没有语音输入 |
| 变声 | ❌ **MVP 不需要** | 没有语音（见 13.1） |
| TTS | ❌ **MVP 不需要** | 纯文字（见 13.1） |

#### 安全与合规（MVP）

| 类别 | 选型 | 说明 |
| --- | --- | --- |
| 身份 | 手机号实名 + 匿名 ID 分离 | 前台匿名 / 后台可追溯 |
| 加密 | TLS + AES-256 | 传输与必要存储 |
| 沙箱 | iframe + CSP | Widget 隔离 |
| 留存 | **常规即焚 + 危机/举报两个透明例外** | 见 13.3 |
| 合规 | 备案 + 实名 + 内容审核 | 类目资质需**前置确认** |

### 14.2 愿景层（方向对，今天不做）

| 类别 | 愿景选型 | 前置条件 |
| --- | --- | --- |
| 个人代理端侧化 | 手机 / 手表 / 耳机端侧运行 | 端侧小模型能力 + 功耗达标 |
| 端侧隐私 | TEE / 安全芯片 | 移动端 TEE 开放度提升 |
| 身份 | DID + VC | 需解决「DID ↔ 实名」受控映射与合规论证 |
| 隐私证明 | ZKP | 与「监管可追溯」的张力需先解决 |
| 语音 | 文字 + TTS / 端侧变声（三选一，见 13.1） | MVP 文字验证通过后再评估 |
| 代理间协作 | A2A | 协议成熟 |
| 生成式 UI 增强 | AG-UI 等 | 生态成熟 |
| 总线规模化 | Kafka / NATS | 单机 Redis 成为瓶颈时 |
| 长时任务 | MCP Tasks 扩展 | 宿主支持度提升（尚未进官方支持矩阵） |

> **原则**：愿景层的东西**不进 MVP 排期**，只保证架构上不被堵死
> （例如代理接口、授权模型按可迁移设计）。

### 14.3 服务端与现有工程的对应关系

**可直接复用（无需重写）：**

| 需求 | 现有资产 | 复用方式 |
| --- | --- | --- |
| 实时陪伴消息 | `ChatHub` / `IMessageTransport` / `SignalRSignalingService` | 直接复用 |
| 在线池 | `IUserConnectionRegistry` / `INodeRegistry` | 换 Redis 实现即可支撑多实例 |
| 离线消息 / 超时释放 | `IOfflineMessageStore` | 复用同类机制 |
| **AI 缓冲接住** | **`ZLongChat.AiBot`** | **直接复用** —— 它本来就是「AI 以 userId 接入 Hub」，正是 AI 缓冲的形态 |
| AI 能力抽象 | `IAiAssistant` / `MicrosoftExtensionsAiAssistant` | 复用；换 provider 不改代码 |
| 客户端配置下发 | `ZLongChat:Client` + `/api/client-config` | 复用；扩展后下发给 MCP 客户端 |
| 契约 | `ZLongChat.Contracts` | 复用；扩展 Signal / Session / 情绪模型 |
| 公共构建 | `Directory.Build.props` / `Directory.Packages.props` | 复用 |

**需要新增的项目：**

| 新项目 | 职责 | 依赖 |
| --- | --- | --- |
| `ZLongChat.Mcp` | MCP Server：工具集 + UI 资源（`ui://`），**薄层**，调用现有业务服务 | Contracts |
| `ZLongChat.Proxy` | 个人情绪代理（**MVP 作为服务端组件**） | Contracts / Ai |

**需要新增的服务**（放在 `ZLongChat.API/Services/`，沿用现有「接口 + 内存实现」风格）：

| 服务 | 职责 |
| --- | --- |
| `ISignalBus` | 信号发布 / 广播 / 过期（起步内存 + Channel，可换 Redis Streams） |
| `IRoomStore` | 临时房生命周期、倒计时（**服务端权威**） |
| `IMatchingService` | 标签匹配 + 在线池选人 |
| `ICrisisDetector` | 危机识别（语义 + 规则双通道） |
| `IReportStore` | 举报与证据留存 |
| `IQuotaService` | 倾诉 3 次 / 倾听 5 次限次 |

**AI 缓冲跑在哪：服务端，不是端侧。** 理由：
1. 端侧小模型做不到可用的共情对话质量；
2. 危机识别必须在服务端才能保证 100% 触发 + 人工介入；
3. 端侧推理对可穿戴设备算力 / 功耗要求不现实。

端侧代理（愿景）只负责**采集信号 + 代发 + 管理授权**，不承担对话。

## 十五、冷启动路径

> **排期按「人类为主」的顺序安排**：先让真人与协议跑通，AI 缓冲在第二阶段补上。
> 原稿把「个人代理端侧化」放在第二阶段，会严重拖慢验证 —— MVP 阶段代理先做服务端组件。

### 15.1 阶段 0：合规前置（1–2 周）

• **类目资质调研与主体准备** —— ⚠️ **这是唯一可能让项目归零的风险，必须最先做。**

• 心理顾问介入：话术、危机阈值、SOP 制定并签字。

• 隐私政策与用户协议（含 13.3 的两个透明例外）。

> **不做完这一阶段不要写业务代码。** 写完才发现上不了架是最贵的错误。

### 15.2 阶段 1：协议 + 真人闭环（3–4 周）

**目标：验证「陌生人愿意被接住，也愿意接住别人」这个核心假设。**

• MCP Server：暴露 `publish_signal` / `get_active_signals` / `catch_signal` / `send_message` / `end_session`
• 后端：房间状态机、等待窗口、倒计时（**服务端权威**）、限次
• 生成式轻界面：倾诉发布面板 + 信号详情卡 + 等待窗口 + 临时聊天（**全部文字**）
• 接住流程：**先做纯真人路径，不含 AI 缓冲**
• 内测 50–100 人

**验收**：真人接住率 > 40%、临时房完成率 > 70%

### 15.3 阶段 2：AI 缓冲层（2–3 周）

**目标：解决「夜里没人接」的空等问题 —— 但 AI 只做缓冲，不做陪伴终点。**

• `ai_catch_signal`：秒级接住 + 身份透明标识
• AI 缓冲提示词与边界（只共情不建议、不诊断、不挽留）
• 真人切换机制：真人一就位立即询问是否切换
• **危机识别双通道 + 100% 转介 + 透明告知**

**验收**：首次响应 < 5s、AI 缓冲时长中位数 < 10 分钟、AI 身份识别率 100%

### 15.4 阶段 3：安全合规收口（2 周）

• 举报与证据留存（含透明告知）
• 人工审核后台 + 危机值守 SOP
• 留存例外告知到达率监控

**验收**：可上线

### 15.5 阶段 4：代理与生态（持续）

• 个人情绪代理（**先服务端组件**，验证后再评估端侧化）
• 情境触发（手表 / 耳机等，需设备侧投入）
• 发布 MCP Server 到智能体市场、开放 API
• 合作心理机构 / 公益组织、扩展到校园 / 企业 / 社区
• v2 评估语音方案（见 13.1）

### 15.6 人力与排期对照

| 阶段 | 周期 | 建议人力 |
| --- | --- | --- |
| 0 合规前置 | 1–2 周 | 产品 1 + 心理顾问 1 + 法务（外部） |
| 1 协议 + 真人闭环 | 3–4 周 | 后端 2 + 前端 1 + 产品 1 |
| 2 AI 缓冲层 | 2–3 周 | 后端 1 + AI 1 + 前端 0.5 |
| 3 安全合规收口 | 2 周 | 后端 1 + 安全 1 + 运营 0.5 |
| 4 代理与生态 | 持续 | 视方向投入 |

**MVP 到可上线约 8–11 周**（阶段 0–3），比原稿的「四个阶段」更聚焦。

> **明确不做（MVP 之外）**：端侧代理、TEE、DID/ZKP、语音、A2A、AG-UI、情境触发。
> 这些都不进 MVP 排期，只保证架构上不被堵死。

## 十六、指标体系

> ⚠️ **指标是内部参考，不是对外承诺。**
> 去掉「1 分钟被接住」这类承诺后，指标的角色也变了：
> 它们用于**指导改进**，**不进对外宣传**，也不构成对用户的保证。
>
> **原则：指标不能反向激励损害用户的行为。**（下面会给出反例）

### 16.1 指标分类

| 类别 | 用途 | 是否对外 |
| --- | --- | --- |
| **A 效果指标** | 判断"有没有真的帮到人" | ❌ 不对外 |
| **B 健康指标** | 判断系统是否在正常工作 | ❌ 不对外 |
| **C 安全指标** | 判断底线有没有守住 | ❌ 不对外（但监管可查） |

**没有"北极星指标"** —— 因为一旦设了北极星，就会为了它优化，
而这类产品最该避免的就是"为了数字而扭曲行为"。

### 16.2 效果指标（A）

| 指标 | 参考目标 | 说明 |
| --- | --- | --- |
| 真人接住率（状态 3） | > 40%（试点） | 有人愿意接住的比例 |
| 真人陪伴完成率 | > 70% | |
| 倾诉后情绪改善 | > 30% | **区分 AI 组与真人组分别看** |
| 倾听者 7 日留存 | > 30% | 供给端健康度 |
| 倾听者耗竭率 | < 10% | |

### 16.3 健康指标（B）

| 指标 | 参考目标 | 说明 |
| --- | --- | --- |
| 首次响应中位数 | 观测，**不设目标** | 去掉承诺后不再有 SLA |
| AI 缓冲时长中位数 | **越短越好** | 长了说明真人在缺位，是供给问题 |
| 纯 AI 缓冲占比 | < 20% | 例外路径，不该是主流 |
| 3 分钟等待超时率 | < 5% | |
| 举报率 | < 1% | |

### 16.4 安全指标（C）

| 指标 | 参考目标 | 说明 |
| --- | --- | --- |
| 内容审核覆盖率（状态 2/3） | **100%** | 法定要求，可全量埋点 |
| 安全兜底资源展示可用率 | **100%** | 热线卡片在任何界面都能取到 |
| 举报告知到达率 | **100%** | 用户确实看到了留存告知 |
| 举报处置及时率 | > 95%（≤ 24h） | |

> **关于「危机识别率」**：**不再作为指标**。
> 理由：不宣称识别能力，就不该设识别率指标；
> 设了就会为了数字去调阈值，反而制造误伤。
> 只保留可验证的**「热线资源可用率」**与**「举报告知到达率」**。

### 16.5 不看

• DAU

• 使用时长

• 停留时长

• 消息数

• **AI 对话轮数** —— AI 聊得越多，说明用户越依赖一个不该被依赖的对象。

因为这个产品追求的是**说出来一次，然后回到生活**，不是让人沉迷。

### 16.6 一个反例（说明为什么不能设错指标）

原稿有「AI 缓冲接住率 > 60%」这一条。它看起来是"活跃度指标"，但实际衡量的是
**「有多少人没能及时被真人接住」** —— 把它当 KPI，会激励系统**故意延迟真人匹配**以提升 AI 占比。

**判断一个指标是否安全，看它能不能通过损害用户利益来优化。**

## 十七、风险与应对
| 风险 | 应对 |
| --- | --- |
| 协议标准早期 | 同时支持 MCP + A2A，保持兼容 |
| 监管复杂 | 提前备案，实名后台，内容审核 |
| AI 幻觉 | 明确边界，只共情不给建议，AI 不替代专业服务 |
| AI 冒充人类 | 全程身份透明，AI 缓冲有持续标识，绝不混淆 |
| 人类连接丢失 | 人类倾听者网络为核心，AI 只做兜底，鼓励真人切换 |
| 隐私泄露 | 常规即焚 + 匿名 ID 分离 + 沙箱；两个留存例外**必须透明告知** |
| 供给冷启动 | AI 兜底 + 校园/企业单点突破 + 代理轻提醒 |
| 危机干预 | 人工兜底，热线转介，100% 触发，AI 识别更严格 |
| 成瘾设计 | 无信息流，无限次，无停留时长 |
| 代理信任 | 默认最小授权，用户可查看、可撤销 |
| 设备兼容 | 先支持主流设备，逐步扩展 |
| AI 情感依赖 | 限时陪伴（单次 30 分钟）、鼓励真人连接、不制造依赖叙事 |
---

## 十八、附录
### 18.1 核心文案

**原则：只描述事实，不描述保证。**

| 场景 | 文案 |
| --- | --- |
| 三个状态选择 | "说出来就好 · 被听到 · 需要陪伴" |
| 状态 1 提示 | "只保存在这台设备上，没有别人会看到。" |
| 状态 2 提示 | "会出现在树洞池里，别人可以看到，但不会有人来搭话。" |
| 状态 3 提示 | "会出现在树洞池里，愿意的人可以接住你。**不保证一定有人接。**" |
| 发布成功 | "已进入树洞池" |
| 被看见 | "N 个人看到了你的话" |
| 被听到 | "N 个人听到了你的话" |
| 正在接 | "有人正在接住你……" |
| 已接住 | "有人接住了你" |
| AI 接入 | "你好，我是 AI，暂时还没有人接住你，我先陪着。想找真人随时说。" |
| AI 标识 | （头像旁持续显示"AI"标签） |
| 用户呼叫真人 | "已帮你留意，有人愿意时会提醒你。" |
| 真人接入切换 | "有一位倾听者愿意陪你聊聊了。要不要切换过去？" |
| 用户拒绝 AI | "好的，已停止 AI 回复。有人愿意接住你时会提醒你。" |
| AI 缓冲到期 | "要不要试试和一位真人聊聊？" |
| 等待进入 | "已发送，等待 TA 进入" |
| 等待超时 | "这次接住已释放。" |
| 延时提醒 | "当前会话还剩 02:45，需要延时 15 分钟吗？" |
| 延时请求 | "对方想延长 15 分钟陪伴，是否同意？" |
| 延时同意 | "已延长 15 分钟" |
| 延时拒绝 | "对方暂时不想延长，你们可以继续聊到时间结束。" |
| 结束缓冲 | "对话将在 10 秒后结束" |
| 举报提交 | "举报已提交，感谢你帮助社区安全" |
| **举报留存告知** | **"举报已提交。本次对话将保存至服务端，用于安全审核。"** |
| 正常结束 | "这次陪伴已结束，房间已销毁" |
| 安全兜底（热线卡片） | "你不需要一个人扛。"（配 12356 / 110 / 120，**任何时候都能取到**） |

**❌ 禁止出现的文案**（违反即抬高自身义务）：

| 禁止 | 理由 |
| --- | --- |
| "1 分钟内一定有人接住你" | 承诺响应时长 |
| "我们随时都在" | 承诺持续在线 |
| "我们会识别你的危险" | 承诺危机识别能力 |
| "提供心理支持 / 心理疏导" | 触碰《精神卫生法》资质红线 |
| "你的话被系统接住了" | 暗示系统在监测（且是空话） |
### 18.2 第三方服务
| 类别 | 选型 | 理由 |
| --- | --- | --- |
| 心理热线 | 12356 | 危机转介 |
| 紧急救助 | 110 / 120 | 危机转介 |
| 青少年服务 | 12355 | 校园版转介 |
| 心理机构 | 合作机构 | 危机兜底 |
| 公益组织 | 合作组织 | 倾听者供给 |
### 18.3 团队配置建议
| 角色 | 人数 | 职责 |
| --- | --- | --- |
| 产品经理 | 1 | 需求、PRD、迭代 |
| UI/UX | 1 | 生成式 UI、交互 |
| 前端 | 2 | Web、MCP Apps、动画 |
| 后端 | 2 | 服务、信号总线、房间 |
| AI | 1 | 共情提示词、安全兜底规则 |
| 安全/审核 | 1 | 举报、危机、合规 |
| 运营 | 1 | 冷启动、倾听者培训 |
| 心理顾问 | 1 | 危机干预、伦理 |

> **与原稿的差异**：去掉「AI/音频 2 人」（MVP 无语音），后端从 2 人起。
> 详细排期见 15.6。
### 18.4 一句话总结
一个可被任何智能体调用的情绪接住协议。用户只需要表达，个人情绪代理负责协商，情绪信号总线负责广播，
**AI 秒级接住做情感缓冲，人类倾听者自愿深度连接**，生成式轻界面负责交互，聊完即散，无关系沉淀。

**主次必须说清楚**：人类是最终归属，AI 只是让用户「有个缓冲、被接纳」的那一层。
AI 越成功，用户越快被交给真人 —— **AI 时长是成本，不是成绩。**

没有 App，没有注册，没有主页，没有好友列表。
只有意图、代理、协议、信号、陪伴、消散。

应用会变轻，服务会留下。
界面会消失，连接会留下。